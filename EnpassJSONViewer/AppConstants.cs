namespace EnpassJSONViewer
{
    static class AppConstants
    {
        public const string AppName = "Enpass JSON Viewer";
        public const string LocalDateFormat = "yyyy-MM-dd HH:mm:ss"; // K means timezone
    }
}
